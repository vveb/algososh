export const STATE_SELECTOR = {
  default: '[class*=default]',
  changing: '[class*=changing]',
  modified: '[class*=modified]',
};

export const CIRCLE_PARTS = {
  head: '[class*=head]',
  index: '[class*=index]',
  tail: '[class*=tail]',
};